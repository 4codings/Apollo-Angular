query PostList($offset: Int!, $first: Int!) {
  allPosts(offset: $offset, first: $first) {
    nodes {
      ...postFields
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
    }
    totalCount
  }
}

fragment postFields on Post {
  id
  topic
  headline
  summary
  personByAuthorId {
    id
    fullName
  }
}
