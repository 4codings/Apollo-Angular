query PostList($offset: Int, $first: Int) {
  allPosts(offset: $offset, first: $first) {
    nodes {
      id
      topic
      headline
      summary
      personByAuthorId {
        id
        fullName
      }
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
    }
    totalCount
  }
}
